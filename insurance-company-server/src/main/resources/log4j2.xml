<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="60">

    <!-- Define properties for reuse -->
    <Properties>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %c{1} - %msg%n</Property>
        <Property name="LOG_DIR">logs</Property>
        <Property name="APP_NAME">my-springboot-app</Property>
    </Properties>

    <!-- Appenders -->
    <Appenders>

        <!-- Console Appender with color support -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%style{%d{HH:mm:ss.SSS}}{cyan} [%style{%t}{magenta}] %highlight{%-5level} %style{%c{1.}}{blue} - %msg%n"/>
        </Console>

        <!-- Rolling File Appender for all logs -->
        <RollingFile name="FileAppender" fileName="${LOG_DIR}/${APP_NAME}.log"
                     filePattern="${LOG_DIR}/archive/${APP_NAME}-%d{yyyy-MM-dd}.log.gz">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
            </Policies>
        </RollingFile>

        <!-- Rolling File Appender for ERROR logs -->
        <RollingFile name="ErrorFile" fileName="${LOG_DIR}/${APP_NAME}-error.log"
                     filePattern="${LOG_DIR}/archive/${APP_NAME}-error-%d{yyyy-MM-dd}.log.gz">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
            </Policies>
            <Filters>
                <ThresholdFilter level="error" onMatch="ACCEPT" onMismatch="DENY"/>
            </Filters>
        </RollingFile>

        <!-- Async wrapper to improve performance -->
        <Async name="AsyncConsole">
            <AppenderRef ref="Console"/>
        </Async>
        <Async name="AsyncFile">
            <AppenderRef ref="FileAppender"/>
        </Async>
        <Async name="AsyncErrorFile">
            <AppenderRef ref="ErrorFile"/>
        </Async>

    </Appenders>

    <!-- Loggers -->
    <Loggers>
        <!-- Application-specific packages -->
        <Logger name="com.example" level="info" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
            <AppenderRef ref="AsyncFile"/>
        </Logger>

        <!-- Hibernate / SQL tuning -->
        <Logger name="org.hibernate.SQL" level="warn" additivity="false">
            <AppenderRef ref="AsyncFile"/>
        </Logger>
        <Logger name="org.springframework" level="info"/>

        <!-- Root logger -->
        <Root level="info">
            <AppenderRef ref="AsyncConsole"/>
            <AppenderRef ref="AsyncFile"/>
            <AppenderRef ref="AsyncErrorFile"/>
        </Root>
    </Loggers>

</Configuration>
